import { BookSearchTitlePipe } from './book-search-title.pipe';

describe('BookSearchTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new BookSearchTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
